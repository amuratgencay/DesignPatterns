@model DesignPattern.WebApp.Entity.VmDesignPatterns
<div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="creationalNavbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Creational Patterns<span class="sr-only">(current)</span></a>
            <div class="dropdown-menu" aria-labelledby="creationalNavbarDropdown">
                <a class="dropdown-item" href="~/DesignPattern/CreationalPatterns">Creational Patterns</a>
                <div class="dropdown-divider"></div>
                @foreach (var item in Model.CreationalDesignPatterns.CreationalDesignPatterns)
                {
                    <a class="dropdown-item" href="~/CreationalPattern/@(item.Name)Pattern">@item.Name</a>
                }
            </div>
        </li>
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="structuralNavbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Structural Patterns</a>
            <div class="dropdown-menu" aria-labelledby="structuralNavbarDropdown">
                <a class="dropdown-item" href="~/DesignPattern/StructuralPatterns">Structural Patterns</a>
                <div class="dropdown-divider"></div>
                @foreach (var item in Model.StructuralDesignPatterns.StructuralDesignPatterns)
                {
                    <a class="dropdown-item" href="~/StructuralPattern/@(item.Name)Pattern">@item.Name</a>
                }
            </div>
        </li>
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Behavioral Patterns</a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" href="~/DesignPattern/BehavioralPatterns">Behavioral Patterns</a>
                <div class="dropdown-divider"></div>
                @foreach (var item in Model.BehavioralDesignPatterns.BehavioralDesignPatterns)
                {
                    <a class="dropdown-item" href="~/BehavioralPattern/@(item.Name)Pattern">@item.Name</a>
                }
            </div>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="~/Home/About">About</a>
        </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>
</div>